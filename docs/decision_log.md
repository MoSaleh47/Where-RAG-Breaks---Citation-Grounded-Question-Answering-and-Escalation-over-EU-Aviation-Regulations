# Decision Log

Decisions are append-only. Reversals receive a new entry that references the earlier decision.

| Date | ID | Decision | Evidence/rationale | Consequence | Status |
|---|---|---|---|---|---|
| 2026-07-15 | D001 | Treat the three-system comparison as the baseline and RAG failure/escalation analysis as the primary contribution. | Earlier and latest supervisor feedback plus repository audit. | Advanced RAG experiments become the main empirical work. | accepted |
| 2026-07-15 | D002 | Preserve existing files as an immutable historical baseline. | Current artifacts are needed for continuity and metric auditing. | New work is isolated in `aviation_rag_research/`. | accepted |
| 2026-07-17 | D003 | Repair corpus hierarchy before comparing embedding models. | Current chunks contain truncation, size, identity, and type confounders. | E1 is a gate for E2 and later experiments. | accepted |
| 2026-07-17 | D004 | Define hybrid retrieval as lexical BM25 plus dense semantic retrieval with rank fusion. | Dense retrieval is already semantic; two complementary retrieval channels are needed. | E3 uses BM25+dense RRF. | accepted |
| 2026-07-17 | D005 | Keep agentic RAG optional and constrained. | Supervisor framed agents as future direction; current cross-reference sample is insufficient. | E6 runs only after a validated residual failure. | accepted |
| 2026-07-17 | D006 | Accept parent_child_v1 as the structural E1 corpus. | Twelve tests, zero validation issues, deterministic rebuild, and a stratified manual audit passed. | Legacy QA evidence mapping can begin; embedding changes remain gated on a frozen mapping. | accepted |
| 2026-07-17 | D007 | Preserve atomic short legal/table units and enrich retrieval context instead of applying a blind minimum chunk size. | Short units are meaningful in their hierarchy and fixed merging would recreate boundary confounding. | Monitor short and long units during development-set retrieval error analysis. | accepted |
| 2026-07-17 | D008 | Require explicit legacy-to-E1 lineage and never auto-accept fuzzy-only matches. | 291 QA records map by exact/containment logic; nine required individual review. | Parent mapping outputs include status, candidates, scores, overrides, and hashes. | accepted |
| 2026-07-17 | D009 | Exclude QA_0003 from final claims while retaining the stored 100-record view for continuity. | Its source is table-of-contents text and its answer is tautological; all systems failed its citation criterion. | Report both the immutable N=100 baseline and the N=99 sensitivity view. | accepted |
| 2026-07-18 | D010 | Treat the Legacy-100 evidence review as AI-assisted candidate curation, not completed human validation. | 21 records needed correction and one TOC-derived record needed exclusion. | Final claims still require independent review of exceptions and a stratified unchanged sample. | accepted |
| 2026-07-18 | D011 | Split candidate evaluation data by legal parent rather than QA ID. | Parent overlap would leak nearly identical evidence across tuning and test. | Candidate split has 42 development and 57 test records with zero parent leakage. | accepted |
| 2026-07-18 | D012 | Do not circumvent the environment's external-data-export gate for embeddings. | The API request was denied before transmission because corpus and QA text require explicit export authorization. | Continue local BM25 work; dense experiment remains ready but unrun. | accepted |
| 2026-07-20 | D013 | Run the two planned embedding experiments after informed user authorization. | The user explicitly authorized exporting 1,535 public EASA retrieval texts and 99 synthetic audited questions to the OpenAI Embeddings API and accepted API charges. | Both model-specific vector sets are cached; 345,642 total input tokens were processed. | accepted |
| 2026-07-20 | D014 | Select text-embedding-3-large as the dense baseline using development data only. | It exceeded text-embedding-3-small exact-child recall at development k=5 and k=10 and improved evidence recall and nDCG. | Test metrics are reported only after the selection; the larger model's gain is described as modest. | accepted |
| 2026-07-20 | D015 | Select equal-weight BM25 plus large-embedding RRF as the candidate retrieval default. | With a fixed RRF constant of 60, hybrid test exact-child recall is 80.7% at k=5, 87.7% at k=10, and 94.7% at k=20. | Retrieve 20 candidates, rerank to a small evidence set, and do not feed all candidates blindly to generation. | accepted |
| 2026-07-20 | D016 | Treat the three hybrid k=20 residuals as benchmark-audit targets before calling them a system ceiling. | QA_0063 is ambiguous, QA_0080 has equivalent repeated annex evidence, and QA_0176 is a broad query-to-evidence alignment failure. | Human review must distinguish true retrieval failures from incomplete or ambiguous gold labels. | accepted |
| 2026-07-22 | D017 | Reject parent-mass score boosting and unequal RRF weights as the reranking intervention. | Development-only checks showed parent boosting consistently reduced recall; unequal weights did not beat equal RRF for the top-20 candidate objective. | Use a true query-passage reranker and preserve these as negative ablations. | accepted |
| 2026-07-22 | D018 | Use the local MiniLM MS MARCO cross-encoder as the candidate top-20 reranker. | It is a lightweight fixed English passage-ranking model, runs locally, improves development and test ranking metrics, and sends no project text to hosted inference. | Hybrid plus cross-encoder becomes the candidate retrieval stack; multilingual reranking remains pending scope approval. | accepted |
| 2026-07-23 | D019 | Constrain generation to retrieved source IDs, app-resolved canonical citations, and verbatim evidence quotes. | Model-written citation strings cannot be trusted as provenance; structural verification can deterministically reject missing or altered quotes. | Generation outputs use a strict JSON contract, while semantic support still requires separate evaluation. | accepted |
| 2026-07-23 | D020 | Prepare compact top-5 and neighbour-enriched context variants without running final generation. | Context selection can be implemented and audited before reviewed gold exists, while final test answer calls would prematurely expose and score an unfrozen benchmark. | Generation remains blocked until independent review is incorporated. | accepted |
| 2026-07-23 | D021 | Represent reviewed evidence as one or more acceptable evidence sets and freeze only after strict review validation. | Several legal questions may have equivalent one-child or multi-child support; a single gold-child list would misclassify valid retrieval. | Retrieval metrics accept alternative evidence routes, and the freeze command rejects incomplete reviews and regenerates grouped splits. | accepted |
| 2026-07-23 | D022 | Map the research narrative into the six mandatory aivancity report chapters while preserving granular experiment subsections. | The school template requires Introduction, State of the Art, Project Context, Methodology, Results, and Conclusion unless the academic supervisor approves a different macro-structure. | The LaTeX source follows the official structure; the earlier research-oriented contents remain a subsection-level writing map. | accepted |
| 2026-07-25 | D023 | Replace single-reviewer adjudication with a multi-reviewer panel of practising aviation professionals, collected through a bilingual FR/EN form, under an aggregation rule fixed before collection. | A single author-reviewer cannot answer the objection that the benchmark is AI-generated and AI-audited; independent raters yield a measurable agreement statistic. | Records clear the validity gate only under the pre-registered rule: minimum two raters, strict majority on the overall decision and on each of the four validity checks, `accept` only when all four checks resolve to yes. | accepted |
| 2026-07-25 | D024 | Never auto-apply a panel "accept with correction" verdict; contested and correction-flagged records are adjudicated by the author and reported separately from panel-cleared records. | Corrected evidence IDs cannot come from a multiple-choice form, and inter-rater disagreement is itself evidence that a question is ambiguous rather than noise to be resolved away. | Agreement statistics and the list of contested records are reported in the results and limitations chapters. | accepted |
| 2026-07-25 | D025 | Fix the review collection cutoff at 6 August 2026 and freeze the benchmark on whatever coverage exists at that date, without extension for late responses. | A freeze date that moves whenever a response is outstanding is not a schedule; and a cutoff chosen after seeing the response rate would let coverage be selected to favour a preferred result. | Records below the two-rater minimum at the cutoff fall to author adjudication under D024 and are reported as author-adjudicated rather than panel-cleared. Response counts, per-record rater coverage, and the cutoff date are reported exactly as observed. Late responses received after the freeze are retained and reported as a robustness check, never merged into the frozen set. | accepted || 2026-08-08 | D026 | Repair three internally inconsistent adjudication rows before freezing, without changing any panel-cleared verdict. | Verification against the freeze contract found QA_0071 marked `accept` while carrying a corrected answer and a 22-child corrected evidence set that the applier would have discarded; QA_0003 marked `exclude` while carrying speculative alternative children; QA_0183 marked `correct` with all four validity checks set to yes. | QA_0071 becomes `correct` with `answer_fully_supported`, `citation_correct` and `gold_evidence_complete` set to no, following the author's own flag convention on QA_0081 and QA_0213; its gold evidence becomes the 22 children of Annex I Section 1 and its citation becomes "Annex I, Section 1". QA_0003's unused evidence fields are cleared and the candidate IDs preserved in `review_notes`. QA_0183's `answer_fully_supported` becomes no. The as-exported file is retained at `review_kit/out_final/review_queue_as_exported_2026-08-08.csv`. | accepted |
| 2026-08-08 | D027 | Freeze the reviewed benchmark at 98 records. | All 33 queued records carry a decision and a one-line justification: 14 panel-cleared under D023, 19 author-adjudicated under D024, collection closed at the D025 cutoff. The freeze script validated the queue against the review contract and rejected nothing. | Benchmark drops from 99 to 98 records: QA_0003 (already `excluded_invalid`) and QA_0063 (test split, no reviewer accepted it) are excluded. Decisions are 19 accept, 12 correct, 2 exclude. Eight questions were reworded, so cached question embeddings and rankings for those eight require recomputation; no parent assignment changed. The regenerated grouped split is 40 development / 58 test over 60 legal parents with zero parent leakage, and is not identical to the earlier 42/57 candidate split. Frozen artifacts and hashes are in `data/evaluation/reviewed_benchmark_v1/`. | accepted |
| 2026-08-08 | D028 | Inherit the candidate development/test assignment for every retained record instead of recomputing the grouped split at freeze time. Supersedes the split reported in the first execution of D027. | `stable_grouped_split` orders legal parents by group size and fills greedily toward `round(N x 0.4)`. Excluding QA_0063 changed both the target and the group ordering, and the recomputed partition moved 12 records across the development/test boundary. Every prior selection decision -- D014 (embedding model), D015 (RRF configuration), D017 (rejected boosting and unequal weights), D018 (cross-encoder) -- was made on the earlier development set, so records that moved into test had already been used for selection. Recomputing would have retroactively contaminated the test partition. | `scripts/freeze_reviewed_benchmark.py` gains `--inherit-splits`, which carries assignments over and refuses any retained record without one; the pre-patch script is kept at `scripts/_superseded_freeze_reviewed_benchmark_2026-08-08.py`. The frozen split is 42 development / 56 test over 60 parents, zero leakage, no record on a different side than before. The development fraction is therefore 0.429, not 0.400; report the observed fraction, not the requested one. `freeze_summary.json` records `split_provenance` with the source manifest hash. The first freeze, with the recomputed split, is retained at `data/evaluation/_superseded_reviewed_benchmark_v1_recomputed_split_2026-08-08/`. | accepted |
| 2026-08-08 | D029 | Publish the reviewed retrieval figure without the candidate watermark, and only that figure. | The watermark asserts that independent review is pending. D027 froze the benchmark, so leaving it on the reviewed assets would be false; removing it from the candidate assets would be equally false, because those are still scored on AI-assisted labels. | `scripts/build_candidate_report_assets.py` gains `--reviewed`, which switches the titles, drops the watermark, and writes `reviewed_*` filenames so candidate assets can never be overwritten; the default path is unchanged. Two hard-coded strings were also fixed: the panel-B axis label said "Candidate" regardless of input, and the ceiling annotation read "54/57" as a literal -- it is now derived from the data and drawn only when the two systems' k=20 recall is genuinely equal (now 54/56). The pre-patch script is at `scripts/_superseded_build_candidate_report_assets_2026-08-08.py`. Rebuilding the candidate assets reproduces a byte-identical CSV; the PNG differs by roughly one pixel of annotation placement, which is placement, not data. | accepted |
| 2026-08-08 | D030 | Let D014 stand: `text-embedding-3-large` remains the dense baseline, and the selection is not re-made on reviewed labels. Disclose that the evidence for it weakened. | Re-scored on reviewed development data, 3-large still beats 3-small on five of six criteria (any-gold-child recall and nDCG at k=5, evidence recall at k=5 and k=10, nDCG at k=10) but loses any-gold-child recall at k=10: 0.762 against 0.786. Under candidate labels it led at both cutoffs. | The choice is not re-fitted, because re-selecting after seeing reviewed labels would make the selection depend on data that now also grounds the reported results. The operating point is k=5 followed by reranking, where 3-large still leads. Chapter 5 must state the k=10 reversal explicitly rather than reporting only the five criteria that hold; the honest reading is that the two embedding models are closer than the original margin suggested. | accepted |
| 2026-08-08 | D031 | Replace substring citation matching with two defined measures -- parent-level resolution against the corpus, and exact unit match including the sub-paragraph -- and report the original scorer beside them rather than instead of them. | The original `citation_match` returned 1.0 for substring containment, so gold `Article 4` matched inside `Article 4(6)` and gold `Section 1` inside `section 1.2`, and returned 0.5 for naming the article number alone, which then passed the `>= 0.5` correctness threshold. Fifteen few-shot records counted correct under it cite the wrong sub-paragraph of the right article, for example Article 18(5) where the provision is Article 18(2). | `src/aviation_rag/evaluation/citations.py` parses citations and resolves them against `parents.jsonl`, so only parents that exist can be credited; 29 tests pin the behaviour, including two that assert the old scorer's errors so the change stays auditable. Where a gold citation does not name its regulation, every regulation holding that provision counts, so no system is penalised for a distinction its label never made. Reported effect on the three-system baseline: few-shot k=10 falls from 0.260 to 0.064 exact, LoRA from 0.178 to 0.111, RAG k=5 from 0.671 to 0.508. The ranking RAG > few-shot > LoRA is unchanged on both splits. | accepted |
| 2026-08-08 | D032 | Re-score the three-system baseline on the 73 records whose question text is unchanged, and report the exclusion as a selection effect rather than as a technicality. | Twenty-five of the 98 frozen records have a question the stored predictions never saw: 8 reworded during the independent review and 17 reworded earlier by the AI audit under D010. Scoring a stored answer against a question that was never asked measures nothing. | The primary comparison is n=73, of which 63 have a gold citation that resolves to a provision. The excluded IDs are listed in `baseline_metrics.json`. Because the audit rewrote questions it judged vague or compound, the surviving 73 are plausibly better posed than the full set, so the baselines may score higher on them than they would on all 98; Chapter 5 must state that direction. A clean n=98 comparison needs generation re-run on the frozen wording and is blocked pending the hosted-generation authorisation. No model was called: `model_calls: 0` is recorded in the run manifest. | accepted |
| 2026-08-08 | D033 | Report the citation-legibility defect as a measured property of the benchmark, not as an anecdote from reviewer comments. | Resolving every gold citation against the corpus shows that only 48 of 98 labels identify the parent holding their own evidence: 28 name an Article while the evidence sits in a GM section, 12 omit which of the four regulations they mean, and 10 cannot be parsed into any provision at all. | 22 of 98 records carry a label that cannot be resolved to one provision from the string alone -- about four times what the six free-text reviewer comments suggested. All of them resolve correctly inside the pipeline, which carries the parent identifier alongside, so no internal metric can see the defect; only a reader holding the citation string can. The 28 guidance-material records are a legitimate indirection rather than an error, but they mean that citing correctly and retrieving the evidence are different questions on nearly a third of the benchmark. Records with unresolvable labels are excluded from the citation denominators rather than scored zero, so the system is not blamed for a defect in the benchmark. | accepted |
| 2026-08-08 | D034 | Run evidence-constrained generation (E5) after informed user authorisation, superseding the block recorded in D020. | D020 blocked generation for two reasons: the benchmark was unfrozen, and hosted generation had never been authorised. D027 froze the benchmark, and the user has now explicitly authorised sending the retrieved public EASA source blocks and the benchmark questions to the OpenAI chat completions API and accepted the charges, on the same basis as the embedding authorisation in D013. | Generation proceeds under the D019 contract: sources supplied by retrieval only, every claim bound to a supplied source_id and a verbatim quote, no model-written display citations. The API's strict structured-output mode is derived from `schemas/answer_output_v1.schema.json` at request time; the canonical schema on disk is unchanged and every response is re-validated against it locally, so a response that satisfies the API but breaks the project contract is still recorded as a failure. | accepted |
| 2026-08-08 | D035 | Use `gpt-4o-mini` as the E5 generator, at temperature 0, with the resolved dated snapshot recorded in the run manifest. | The few-shot and RAG baselines were produced with `gpt-4o-mini`, so holding the generator fixed makes E5 against those baselines a controlled comparison in which only the scaffolding changes -- retrieval quality, the evidence constraint and the JSON contract. This answers the same-base-model comparison the supervisor raised on 29 July. A weaker generator is also the appropriate instrument here: the object of study is when to escalate, and a frontier model would suppress the failures the escalation policy exists to detect. | The reported gain over the RAG baseline is attributable to the method rather than to model capability. The trade-off is that absolute answer quality is not state of the art, which must be stated rather than implied; a stronger current model is a documented extension, not a correction. `gpt-4o-mini` was confirmed still supported at the time of the run and is not on the deprecation schedule. | accepted |
| 2026-08-08 | D036 | Generate and calibrate on development only, and enforce that in the tooling rather than by intention. | Verifier behaviour and abstention thresholds are selection decisions; making them while able to see held-out outcomes would contaminate the test partition, which no later disclosure can repair. | `scripts/run_generation.py` defaults to `--split development` and refuses `test` or `all` unless `--i-have-frozen-calibration` is also passed. The held-out split is generated once, after thresholds are fixed. Responses are cached by a key over model, prompt, schema and context, so the single test run is reproducible without re-billing. | accepted |
| 2026-08-08 | D037 | Define a verbatim quote as a contiguous word run matching the source after punctuation and typography normalisation, and report the byte-level result alongside as a second figure. | Of 132 development quote spans, 90 were byte-exact, **zero** were repaired by unicode or curly-quote normalisation, 31 differed only in punctuation, and 11 were genuinely wrong. The 31 are ordinary quotation -- ending early and closing with a full stop where the source continued with a comma, or dropping a leading paragraph marker such as `4.` -- and the corpus is a PDF extraction carrying its own artefacts, `safetyrelated` appearing unhyphenated in the source text, so byte-exactness partly measures the extractor rather than the model. | The choice moves the reported grounding rate by 21 points, so both are always reported: `quote_pass` under the normalised rule and `quote_pass_strict` under the original. Word order is never relaxed, because contiguity is what rejects elision. `src/aviation_rag/generation/verification.py` gains `quote_is_grounded`, `quote_is_verbatim` and `contains_elision`; four tests pin that punctuation may vary, word order may not, paraphrase is still rejected, and elided quotes are rejected and flagged. The previous implementation is kept at `_superseded_verification_2026-08-08.py`. | accepted |
| 2026-08-08 | D038 | Adopt prompt v2, which forbids elided and spliced quotes, selected on development data; keep v1 on disk as the comparison. | v1 asked for a verbatim quote without saying what that excluded, and produced elided quotes in 7 of 42 development answers; every elision removed material from a legal test, one deleting an entire limb of an exception while reading fluently. v2 changes one rule and nothing else. | Development grounding rises from 32/42 to 40/42 under the normalised rule and from 23/42 to 31/42 byte-exact; marked elisions fall from 7 answers to 0. Claims fall from 124 to 114 while quote spans hold at 132 and 133, so the constraint appears to suppress ungroundable claims rather than degrade answers. This is development-set selection and must be described as such; the held-out split will use v2 and that will be its first exposure to unseen data. | accepted |
| 2026-08-08 | D039 | Treat structural contiguity, not ellipsis detection, as the load-bearing verification check; and drive abstention from external verification rather than from the generator's own judgement. | Two development observations. First, prompt v2 eliminated every marked elision but introduced one unmarked splice: on QA_0294 the model joined two fragments with no marker, deleting the clause limiting a provision to persons directly involved in production. A regular expression for ellipsis characters would have passed that answer; only contiguity matching rejects it. Second, the model abstained 0 times in 84 opportunities across both runs, despite a schema that offers four abstention reasons at no cost and answers that were demonstrably ungrounded, including one fabricated prohibition. | A prompt constraint can displace a failure mode into a less visible form rather than remove it, which is an argument for verification over instruction. Self-reported abstention is unusable as a signal, so the escalation layer must key on deterministic verification outcomes. Both observations belong in Chapter 5 and the second in the conclusion, because it converts abstention from a design preference into a measured necessity. | accepted |
| 2026-08-08 | D040 | Do not fit escalation thresholds on the current development split; report the calibration as underpowered and state the sample size that would be required. | The development base rate of a wrong answer is 9 in 42. Every candidate rule's Wilson interval on precision contains or nearly contains that 0.21 base rate: the best observed, escalating when the reranker moves the top-1 result by two or more ranks, gives precision 0.42 [0.23, 0.64] at recall 0.89. A threshold fitted to nine events would be indistinguishable from noise and would not survive a question about how it was chosen. | The policy is specified but its retrieval-uncertainty thresholds are left explicitly open. A two-proportion power calculation at alpha 0.05 and power 0.80 gives the requirement: demonstrating the observed 0.42 precision against the 0.21 base rate needs about 80 escalated decisions, roughly 178 questions at the observed 45% escalation rate -- against a frozen benchmark of 98. Chapter 5 reports the negative result and the power requirement rather than a fitted number, and escalation precision and recall are reported together for every rule. | accepted |
| 2026-08-08 | D041 | Keep quote verification and evidence-selection checking as two independent layers, on measured rather than assumed grounds. | On development the two failure modes do not overlap at all: 9 answers selected the wrong evidence, 2 answers failed quote verification, and **zero** answers did both. Quote verification has precision 0.00 and recall 0.00 for predicting evidence-selection errors, and the two answers it rejected had entirely correct evidence. | A verifier checking only quotes would have passed all nine evidence errors; a check on evidence selection alone would have passed both quote failures. Quote fidelity is deterministically checkable at runtime with no gold labels and gates the answer directly. Evidence selection is not observable at runtime and must be predicted from retrieval signals, which D040 shows cannot yet be done reliably. The architecture therefore keeps both layers, and the mémoire justifies that from this measurement rather than from design intuition. | accepted |
| 2026-08-08 | D042 | Gate answers on quote-verification failure without calibration, and state the cost assumption behind any escalation rate explicitly. | Quote verification needs no threshold: a quote either appears contiguously in the cited source or it does not. It has no false positives by construction, so it can gate an answer immediately. Retrieval-uncertainty escalation is a different matter -- a rule escalating 45% of questions to catch 89% of errors may be correct for aviation regulation, where a wrong citation is expensive and a human referral is cheap, but that is a judgement about the cost ratio and not something the data decides. | Level assignment: quote-verification failure blocks the answer unconditionally; retrieval-uncertainty escalation is specified with thresholds open pending D040; model self-abstention is excluded as a signal entirely under D039. Wherever an escalation rate is reported, the assumed relative cost of a wrong citation against a referral is stated in the same sentence, so the number is never presented as self-selecting. | accepted |
| 2026-08-12 | D043 | Generate once on the held-out partition, with everything it evaluates frozen beforehand and nothing calibrated afterwards. | Generation results reported only on development data leave the strongest chapter without a held-out number. The components the held-out run evaluates are all fixed: prompt v2 (D038), the contiguity definition of a verbatim quote (D037), the unconditional quote-verification gate (D042), the retrieval stack (D014, D015, D018), and the benchmark itself (D027). The one component that is *not* fixed -- the retrieval-uncertainty escalation threshold -- is deliberately left open under D040 and is therefore not evaluated on held-out data at all. | Frozen before the run: `prompts/evidence_constrained_answer_v2.md`, `src/aviation_rag/generation/verification.py`, and the reviewed benchmark. Reported from the run: schema conformance, quote verification under both the normalised and byte-level rules, elision counts, abstention counts, and evidence-selection correctness. **No threshold, prompt or model choice may be revised in response to these numbers.** If the held-out result is worse than development, that is the result and it is reported as such. Responses are cached by model, prompt, schema and context, so the run is reproducible without re-billing and cannot be quietly repeated with different settings. | accepted |
| 2026-08-12 | D044 | Report the held-out generation result exactly as observed, including the two respects in which it is worse than development, and revise nothing in response to it. | The held-out run under D043 gives quote grounding 49/56 (87.5%) against 40/42 (95.2%) on development, byte-exact 36/56 against 31/42, elided quotes in 4 of 56 answers against 0 of 42, one schema-contract violation against none, and evidence selection correct in 47/56 (83.9%) against 33/42 (78.6%). Wilson intervals on the grounding rates overlap ([0.764, 0.938] against [0.842, 0.987]), so the drop is not distinguishable from sampling variation at this size; the elision recurrence is not a matter of interval overlap but a qualitative reappearance of a failure mode that development showed as absent. | Two claims in Chapter 5 must be weakened rather than restated. First, prompt v2 did **not** eliminate elision: the development zero was optimistic, and on unseen questions the rule reduced the failure without removing it. Second, the generation numbers are reported with both splits side by side, never development alone. Evidence selection was in fact better on held-out data, which is reported with equal prominence so the comparison is not presented selectively. No prompt, threshold or model was changed after seeing these numbers, as D043 required. | accepted |
| 2026-08-12 | D045 | Treat the single held-out schema violation as a principal result rather than an error rate, and keep application-side contract validation permanently. | On QA_0017 the model emitted the source identifier `EU_376_2014_ART_5__C001` where the supplied identifier was `EU_376_2014_ART_5__C0001` -- a fabricated reference differing from a real one by a single omitted character, inside a response that the API's strict structured-output mode had already accepted. | Strict structured output guarantees the *shape* of a response: that a field exists and holds a string. It cannot guarantee that the string is a value the application supplied. The distinction is invisible until a model produces a plausible near-miss identifier, which is precisely what a human reviewer skimming an answer would also miss and what the deterministic allowed-source check caught immediately. The mémoire states this as the difference between a schema-conformant response and a contract-valid one, and the architecture retains local re-validation against the project schema after every generation call regardless of what the provider guarantees. | accepted |
| 2026-08-27 | D046 | Add three drawn figures -- the end-to-end pipeline, the escalation ladder with the evidential status of each transition, and a unit chart of the two failure modes -- and replace the co-occurrence table with the last of them. | Academic-supervisor review of 27 August 2026: one figure in 106 pages. The escalation ladder and the co-occurrence counts were each carried by a table, and the escalation table (`tab:escalation-method`) was never referenced in the text. | Figures are authored as TikZ under `memoire/latex/figures/fig_pipeline.tex`, `fig_escalation.tex` and `fig_failure_modes.tex`, so they compile with the document and carry no external image dependency; `main.tex` gains `tikz` with the `arrows.meta`, `positioning` and `calc` libraries. Table `tab:orthogonal` is superseded by Figure `fig:failure-modes`, which shows the same four counts against the 42-answer denominator. `tab:escalation-method` is now referenced from the Chapter 4 text and forward-referenced to the figure. No figure states a quantity not already reported in the surrounding text. | accepted |
| 2026-08-27 | D047 | Open Chapter 5 with a synthesis table of the chapter's principal results, each row naming the section or table where it is reported. | Same review: the chapter's headline quantities were only reachable by reading it end to end. | `tab:results-summary` carries twenty rows over five groups -- corpus and benchmark, retrieval, re-scored baselines, generation, escalation -- with every value cross-referenced rather than restated, so the table cannot drift from its sources without a broken reference. Held-out figures are labelled as coming from the single post-freeze run required by D043. | accepted |
| 2026-08-27 | D048 | Neutralise the register in twenty-six places across Chapters 5 and 6, changing wording only and no quantity, claim or conclusion. | Same review: the tone is assertive in places. Phrasings such as "the correct reading is the uncomfortable one", "it was the wrong one", "more useful than a threshold fitted to nine events would have been" and "this work does not pretend otherwise" argue for the finding rather than reporting it, which invites a reader to discount the finding itself. | Every edit is a substitution of phrasing at the sentence level; no number, interval, denominator or bolded claim was altered, and the substantive claims -- including "a rule stated to a generator is a request; a rule checked against the evidence is a guarantee" -- are retained. Second-person address is removed from the closing remark. | accepted |
| 2026-08-27 | D049 | Extend the three-system comparison from 73 to all 98 frozen records by regenerating the few-shot, RAG and LoRA predictions against the frozen question wording, rather than merging new predictions for the 25 reworded records into the stored 73. | Academic-supervisor review of 27 August 2026. The stored predictions answer the pre-freeze wording, so `rescore_legacy_baselines.py` drops 25 records, and those 25 are the ones the audit and the review judged vague or compound -- not a random subset, and plausibly the harder ones. Regenerating only the 25 would leave a table whose rows were produced months apart under a floating model alias. | `scripts/rerun_legacy_baselines.py` regenerates few-shot k=1/5/10 and RAG k=1/3/5 over the frozen wording with the historical system prompts verbatim, the same exemplar-sampling procedure at seed 42, temperature 0 and the same k values, and pins `gpt-4o-mini-2024-07-18` instead of the `gpt-4o-mini` alias. Retrieval uses NumPy cosine over L2-normalised vectors in place of `faiss.IndexFlatIP` -- the same quantity, one fewer native dependency, ties broken by chunk order. `scripts/rerun_legacy_lora.py` re-runs the adapter unchanged and greedily, and reports how many of the 73 unchanged records reproduce their stored prediction exactly, so the run is also a reproducibility check on the historical artefact. Nothing under `Documents/` is written (D002). Estimated cost of the API portion: USD 0.12 over 593 calls. Chapter 5 will report n=98 with the n=73 column retained beside it, so the effect of the exclusion is visible rather than asserted. | accepted |
| 2026-08-27 | D050 | Run the neighbour-enriched context variant on the development partition only, and refuse a second post-freeze run on the held-out partition. | Same review: the variant is built and never executed, so Chapter 4 specifies a component the results never touch. But the held-out partition was generated once, after every component it evaluates had been frozen (D043), and that single-run discipline is the strongest methodological claim in the thesis. Generating a second configuration on held-out data would retire that claim for an ablation. | The enriched variant is generated on the 42 development records with the same prompt, schema, model and temperature as the compact run, so the two differ only in context construction, and it is reported as a development-only ablation with the held-out comparison explicitly unavailable. `run_generation.py` already enforces this: it refuses `test` and `all` unless `--i-have-frozen-calibration` is passed, and that flag is not passed. The Chapter 6 limitation changes from `built and unrun` to `evaluated on development only`. | accepted |
| 2026-08-27 | D051 | Report the enriched context variant's word budget as the 1,200 words the frozen build actually used, not the 1,600 of the earlier pre-review build, and re-report its statistics on the frozen build. | Preparing the run under D050 exposed a discrepancy between the text and the artefact: `E5_context_top5_neighbors_candidate_v1` was built with `max_words = 1600` over the 99 pre-review questions, while `E5_context_top5_neighbors_reviewed_v1` -- the set generation actually reads -- was rebuilt with `max_words = 1200`. Chapter 4 stated 1,600 and tabulated the pre-review build. | The discrepancy is inert and the text now says why rather than glossing it: no candidate was skipped for budget in the frozen build and the largest context is 1,090 words, so a higher ceiling could not have admitted a further unit and the two settings select the same evidence. The variant is bounded by structure -- fifteen sources is the ceiling for five anchors at radius one -- not by the budget. Table `tab:context-stats` now reports the frozen build (6.27 neighbours, 11.27 sources, 463.8 evidence words per question) with the pre-review figures named in the caption, and Figure `fig:pipeline` no longer shows two different budgets. | accepted |
| 2026-08-30 | D052 | Report the extended baseline comparison as a decomposition -- stored predictions on 73 records, regenerated predictions on the same 73, and regenerated predictions on all 98 -- rather than replacing one table with another. | Two things changed at once when the comparison was extended under D049: the predictions were regenerated and the record set grew. A single before-and-after table would confound run-to-run variation with the effect of the excluded records, and the second is the quantity the supervisor's point is about. | The middle column is a control and it holds: regenerated RAG reproduces the stored predictions on the same 73 records to within one record on every measure, and `rag/1` is identical on all four. Few-shot moves by up to two records, which is expected because its exemplars are drawn at random and the draw sequence changes with the record set. With provenance thus controlled, the record-set effect is isolated: RAG at k=5 falls from 0.730 [0.610, 0.824] parent-level on the 73 kept to 0.360 [0.202, 0.555] on the 25 excluded, intervals not overlapping, and to 0.625 over all 98. The draft's scope note predicted this and the prediction is confirmed rather than hedged. | accepted |
| 2026-08-30 | D053 | Report that few-shot moves in the opposite direction on the excluded records, and read it as evidence that the difference is mediated by retrieval -- as a direction, not a result. | On the 25 records the audit reworded for vagueness, few-shot at k=10 reaches exact-citation 0.360 [0.202, 0.555] against 0.079 [0.034, 0.173] on the 73 kept, non-overlapping in the opposite direction to RAG. On that subset the ordering of the two systems reverses. | Few-shot performs no retrieval, so a subset that is harder for a retriever and easier for a model answering from parametric knowledge is what one would expect if the mechanism is retrieval. The reversal itself rests on 25 records and is stated as a direction to investigate rather than a finding, in both Chapter 5 and the conclusion. The headline gap within the RAG column narrows from 22 points to 18 (0.625 parent against 0.443 exact) and the conclusion, abstract and resume are updated to the full-benchmark figures. | accepted |
| 2026-08-30 | D054 | Execute the neighbour-enriched context variant on development, report it, and do not adopt it. | Required by D050. The run is directly comparable with the compact development run: identical prompt, schema, model, temperature and question set. | The enriched context costs 79% more input tokens (125,205 against 70,089) and returns one record on evidence selection (34/42 against 33/42; 0.810 [0.667, 0.900] against 0.786 [0.641, 0.883]) and one on byte-exact quoting, while reintroducing an elided quote the compact run did not produce. It also yields more claims (126 against 114) and more distinct cited sources per answer (2.98 against 2.60) at an identical 95.2% grounding rate -- more evidence in the window produced more assertions rather than better-grounded ones, offered as an observation from one development run. Nothing here justifies adoption and nothing rules the variant out; the compact context remains the configuration the held-out result stands on. | accepted |
| 2026-08-30 | D055 | Report the two runs of 30 August as computed from their own token counts rather than as reconciled against a billing export. | The provider's cost export used in Appendix A predates these runs, so presenting their cost alongside the reconciled charges without distinction would misstate the provenance of two of the eight rows. | Appendix A now carries eight charges totalling USD 0.2877 against the previous 0.1741, with the two new rows computed from the token counts the runs report at the same unit prices and labelled as not yet reconciled. Embedding tokens rise from 349,114 to 407,876 with the 127 legacy chunks and 98 questions the baseline re-run embedded. The consistency check is restated as covering five of the first six charges. | accepted |
| 2026-08-30 | D056 | Audit the mémoire against the aivancity PFE writing guidelines and fix every mechanical non-conformance found. | The guidelines set requirements the draft had not been checked against: front-matter order, abstract and résumé word limits, cross-referencing of every float, equation referencing, minimum font size inside figures, and bibliography composition. | Front matter reordered to Acknowledgements, Abstract, Résumé, Declaration, per the mandatory structure. Résumé cut from 339 to 300 words (limit 300); abstract already at 288. Twenty floats and equations that were never referenced in the text now carry a lead-in reference before they appear, and `tab:split` was given a forward reference; the guidelines require every figure and table to be referenced before it appears and no table to stand without commentary. Font sizes inside the three drawn figures raised from `\scriptsize` to `\footnotesize` so that, after the `\resizebox` to text width, no glyph renders below the 9 pt minimum. Body length is 76 pages against the permitted 60--90. | accepted |
| 2026-08-30 | D057 | Add two academic books and one item of technical documentation to the bibliography, cited where the work actually rests on them, rather than leave the source-composition minima unmet. | The guidelines require at least 2 academic books or book chapters and 4 items in the `other` category. The bibliography held 19 journal articles, 42 conference papers and 3 other items, and no books. | Added Manning, Raghavan and Schütze, *Introduction to Information Retrieval*, cited in Section 4.4 where term weighting and inverted indexing are specified; Gwet, *Handbook of Inter-Rater Reliability*, cited in Section 5.2 alongside Fleiss for the variable-rater generalisation actually used; and the provider's structured-output documentation, cited in Section 5.5 where strict mode is described. Each is cited at the point where the work depends on it, not appended to meet a quota. The author is to confirm he is comfortable standing behind all three at the defence. | accepted |
| 2026-08-30 | D058 | Submit a Ghostscript-generated PDF/A alongside the ordinary PDF, and record what the conversion costs. | The guidelines require a PDF/A archival file. LaTeX with `hyperref` does not emit PDF/A directly, and adding `pdfx` the night before submission risked the build. | Link annotations were first marked printable, then Ghostscript converted to PDF/A-2 with no compatibility violations reported. All 22 fonts are embedded and subset. The conversion preserves 71 of 575 link annotations: external URLs survive, most internal cross-reference links do not. The ordinary PDF is retained and delivered alongside, since it keeps full internal navigation. No formal veraPDF validation was run; the claim is that Ghostscript reports no PDF/A violation, not that an independent validator has passed the file. | accepted |
| 2026-09-12 | D059 | Publish the codebase and quote the excerpts the argument depends on, in the body as well as in an appendix. | An earlier judgement in this project was that adding code listings would push the report past the 90-page limit. That was wrong: the limit excludes appendices, so the objection did not apply to the thing it was raised against. The document contained no code and no repository link, which for a thesis whose central claim is about what a deterministic checker does left the reader no route to the implementation at all. | New Appendix D carries four listings quoted verbatim from the named file and line range: the contiguity definition of a verbatim quote, the support loop that performs the supplied-source and quote checks, exact citation matching with its excluded denominator, and candidate resolution for ambiguous gold citations. Two further excerpts sit in the body where the claims are made -- the held-out split guard under the third design principle, and the contiguity check in the structural-verification section. The listings are generated by slicing the real source files rather than transcribed, so they cannot drift from the code. Body length goes from 74 to 76 pages, still inside 60--90. | accepted |
| 2026-09-12 | D060 | Exclude reviewer identities from version control before the first commit rather than after it. | The repository was about to be initialised. The existing `.gitignore` covered credentials, caches and generated data but not `review_kit/_private*/` or `reviewer_key_PRIVATE.csv`. Removing a file in a later commit does not remove it from history, so the ordering mattered more than the content of the rule. | `.gitignore` extended to exclude both private review directories, any path matching `*PRIVATE*`, and the provider billing exports. Verified by matching the rules against the actual paths before any commit existed. Appendix A states plainly that the pseudonym-to-person mapping is held outside version control and has never entered the repository's history, so the claim is checkable by a reader who clones it. | accepted |


## Pending decisions for 29 July

- multilingual scope: French queries over English sources, bilingual responses, or bilingual corpus;
- third embedding candidate;
- gold-set size and expert-review availability;
- minimum same-base-model comparison;
- prototype expectations;
- exact mémoire requirements and review date.

