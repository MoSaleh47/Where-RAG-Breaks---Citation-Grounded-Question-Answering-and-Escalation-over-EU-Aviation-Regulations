# Raw Data

Immutable source snapshots belong here only when a deliberate migration is performed. The current official XML/PDF files remain in `../../../Documents/` and are identified by SHA-256 in `../manifests/source_manifest.csv`.

Never modify a raw source in place. A new regulatory revision receives a new file, version identifier, retrieval index, and evaluation-impact note.

